<UserControl x:Class="FreeTrainSimulator.Toolbox.Views.LocationToolView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:FreeTrainSimulator.Toolbox.ViewModels"
             xmlns:views="clr-namespace:FreeTrainSimulator.Toolbox.Views"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LocationToolWindowViewModel, IsDesignTimeCreatable=False}"
             views:ToolWindow.DefaultFloatingSize="520,440"
             d:DesignWidth="520"
             d:DesignHeight="440">
    <ListView Margin="6"
              ItemsSource="{Binding Rows}"
              ItemContainerStyle="{StaticResource ToolWindowRowItemStyle}">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Name" Width="180" DisplayMemberBinding="{Binding Name}" />
                <GridViewColumn Header="Value" Width="320" DisplayMemberBinding="{Binding Value}" />
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
