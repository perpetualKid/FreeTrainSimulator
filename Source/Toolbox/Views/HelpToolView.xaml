<UserControl x:Class="FreeTrainSimulator.Toolbox.Views.HelpToolView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:FreeTrainSimulator.Toolbox.ViewModels"
             xmlns:views="clr-namespace:FreeTrainSimulator.Toolbox.Views"
             xmlns:behaviors="clr-namespace:FreeTrainSimulator.Toolbox.Behaviors"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:HelpToolWindowViewModel, IsDesignTimeCreatable=False}"
             views:ToolWindow.DefaultFloatingSize="480,520"
             d:DesignWidth="480"
             d:DesignHeight="520">
    <DockPanel Margin="6">
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,6">
            <TextBox VerticalContentAlignment="Center"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Search Help" />
        </DockPanel>
        <StackPanel DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    Margin="0,0,0,6">
            <RadioButton GroupName="HelpSearchType"
                         Content="Command"
                         IsChecked="True" />
            <RadioButton GroupName="HelpSearchType"
                         Margin="12,0,0,0"
                         Content="Key"
                         IsChecked="{Binding SearchByKey}" />
        </StackPanel>
        <ListView ItemsSource="{Binding Rows}"
                  behaviors:GridViewColumnBehavior.StretchListView="True">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Command" Width="260" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Key" Width="180" DisplayMemberBinding="{Binding Value}"
                                    behaviors:GridViewColumnBehavior.StretchColumn="True" />
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
