namespace Orts.Menu
{
    #region Menu_Selection enum
    public enum Menu_SelectionIndex
    {
        // Base items
        Folder = 0,
        Route = 1,
        // Activity mode items
        Activity = 2,
        Locomotive = 3,
        Consist = 4,
        Path = 5,
        Time = 6,
        // Timetable mode items
        TimetableSet = 2,
        Timetable = 3,
        Train = 4,
        Day = 5,
        // Shared items
        Season = 7,
        Weather = 8,
    }
    #endregion
}
